using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NetworkedWatchers : MonoBehaviour
{
    // Purely for spawning networked gameobjects that are a part of a prop...

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
